# Copy to .env (gitignored) and fill in. Generate the values with:
#   python -c "import secrets;print(secrets.token_hex(32))"                          # SECRET_KEY
#   python -c "from passlib.hash import argon2;print(argon2.hash('PLAYER_PASSWORD'))"
#   python -c "from passlib.hash import argon2;print(argon2.hash('ADMIN_PASSWORD'))"
SECRET_KEY=
PLAYER_PW_HASH=
ADMIN_PW_HASH=
