### COntrol the object where you start in the level (use the first in the list), or specify random. random is added as a toggle in the editor (and sets variable fixed per level: start_object=-1 (random), 0, 1, ..., last object -1 for all the other object to select). The controllers are adapted to take into account this iunformation when starting a level and alsothe json.
### Stickiness bug (pr of switching away form the object -> then u.a.r between the others). Basically in the editor allows to specify a probabiliy (already there but locked), which moves away form the current chain. capped to 1 of course. it then select one of the object u.a.r (all the objects, even the one finished). add the fact to the controllers that one a chain is finished it can still be selected with the same pr., but cannot advance anymore, but only stay or retroceed. hence a chain it is wlaywas treated normally, even if it finished, but simply cannot advance futrher. game is over as always when the chains are all fully completed.
### Chain never exhaustes completed (i.e. described above)
### Add in the data the name of the trials.jsonl used i.e. paradigm. when logging the data that means saving inside the session_info the name of the .jsonl loaded
### For the lights use colors of red to dark red: #8b0000 and greee to yellowgreen: #CCFF00
### It would be better if for each trial we could specify if the red light is shown at the location of the correct door or at all doors simultaneously. If that were possible, we could set the last trials in a level such that on incorrect trials the red light is shown in all doors, which with the change above would give us a chance to test generalization a second time (without having revealed the location of the correct door on the first trial). That practically means to add to trial-editor per trial whether suggestion or wrong decision final shows the red light in positon or all. it would add simply a toggle called show_all. what it does is that when passsed to the game allows to turn all lights in red. this is a miniaml changes in the code, we need to add new command called all_lights (true or false). the behavour must be the same, maybe some small refactoring would work, understanding what is there, how it is impemnted and how to simplify
### Current door (i.e. one most aligned with) is always bit light up (emissive light on but with 1/3 of the lights). this is a change in game_node for the lights.
### When sending wrong command send command shake camera. this makes the camer shakes a bit like the player has taken damage. the amount of shaking (i..e how much, and how long) it is specified per level. the game knows it must shake when theres a new command sent called shake. it as animation a trigger and runs for its duration
### Add a new visual effect of the bar (put it on the left side of the screen), not too big, which goes from red to yellow (same of the lights), interpolating the colors in between for every correct attempt. bascially it is handled by the controller which sends tha current size of the bar , and decrements and incrments it based on correct and wrong attempt (i.e. check with correct alignment, or check with wrong alignment). that measn in the state we have new iunfos configurable also per level in the editor about the number of ticks inside.
